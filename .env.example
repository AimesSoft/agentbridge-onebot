# AgentBridge HTTP 服务
QQBRIDGE_HOST=0.0.0.0
QQBRIDGE_PORT=8787
QQBRIDGE_WEBHOOK_PATH=/onebot
# NapCat -> AgentBridge 的可选共享 token。NapCat 支持自定义 header 时可发送 X-QQBridge-Token 或 Authorization: Bearer ...
QQBRIDGE_WEBHOOK_TOKEN=
# Hermes agentbridge skill -> AgentBridge 的 token。Hermes 侧必须配置同一个值。
QQBRIDGE_SKILL_TOKEN=

# NapCat OneBot HTTP API
NAPCAT_BASE_URL=http://127.0.0.1:3000
NAPCAT_ACCESS_TOKEN=

# Hermes OpenAI-compatible API
HERMES_BASE_URL=http://127.0.0.1:8642
HERMES_MODEL=hermes-agent
HERMES_TIMEOUT_SECONDS=120
HERMES_SESSION_MAX_AGE_SECONDS=259200
HERMES_SESSION_MAX_HANDOFFS=200

# 机器人身份和管理员
BOT_QQ_ID=
BOT_NAMES=梨花,AgentBridge,bot
ADMIN_QQ_IDS=
ADMIN_PREFIX=。
PUBLIC_PREFIX=/
BOT_PERSONA=你是梨花（NipaPlay）社区的维护者分身。说话自然、简洁、可靠，不自称“作为AI”。

# 群自主互动默认值。单群可在 config.yaml 或管理员命令中覆盖。
GROUP_DEFAULT_AUTONOMOUS_ENABLED=false
GROUP_MIN_SECONDS_BETWEEN_REPLIES=90
AMBIENT_ENABLED=true
AMBIENT_INTERVAL_SECONDS=3600
AMBIENT_MIN_UNREAD_MESSAGES=1
AMBIENT_MAX_UNREAD_MESSAGES=120
AMBIENT_JITTER_MIN_SECONDS=300
AMBIENT_JITTER_MAX_SECONDS=10800
AGENT_RUN_TTL_SECONDS=900
GROUP_ATTENTION_ENABLED=true
GROUP_ATTENTION_TTL_SECONDS=180
GROUP_ATTENTION_BATCH_INTERVAL_SECONDS=8
GROUP_ATTENTION_TICK_SECONDS=1
GROUP_ATTENTION_MAX_BATCHES=8
GROUP_ATTENTION_MAX_BATCH_MESSAGES=20
GROUP_ATTENTION_MAX_BUFFER_MESSAGES=80
GROUP_ATTENTION_MAX_EXTENSION_SECONDS=300
SKILL_ONEBOT_LEVEL=group_admin

# 会话和输出控制
MAX_HISTORY_MESSAGES=16
MAX_GROUP_CONTEXT_MESSAGES=8
MAX_MESSAGE_CHARS=850
USER_RATE_LIMIT_PER_MINUTE=12
STATE_PATH=data/state.json
MESSAGE_STORE_PATH=data/messages.sqlite3
MESSAGE_ARCHIVE_DIR=data/message_archive
QQBRIDGE_CONFIG=config.yaml

# GitHub。建议使用 fine-grained PAT，仅授予 NipaPlay-Reload 所需权限。
GITHUB_TOKEN=
GITHUB_OWNER=AimesSoft
GITHUB_REPO=NipaPlay-Reload
GITHUB_DEFAULT_REF=main
GITHUB_RELEASE_WORKFLOW=release.yml
GITHUB_DEPLOY_WORKFLOW=deploy.yml
GITHUB_API_VERSION=2026-03-10
