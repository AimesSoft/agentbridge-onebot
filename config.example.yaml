bot:
  qq_id: ""
  names:
    - 梨花
    - AgentBridge
    - bot
  admins:
    - ""

github:
  default_repo: default
  repos:
    default:
      owner: AimesSoft
      repo: NipaPlay-Reload
      default_ref: main
      workflows:
        release: release.yml
        deploy: deploy.yml
        ci: ci.yml
    docs:
      owner: AimesSoft
      repo: docs-repo
      default_ref: main
      workflows:
        release: release.yml

groups:
  "example-group-id":
    autonomous_enabled: true
    min_seconds_between_replies: 900
    keywords:
      - NipaPlay
      - 梨花
      - release
      - workflow
      - 构建
      - 发版
